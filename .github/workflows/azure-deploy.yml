name: Azure Deploy

on:
  push:
    branches:
      - main
      - dev

env:
  AZURE_WEBAPP_NAME: salonis-mock-trading-app    # ✅ Your Azure app name

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    # Add environment variables for Django commands
    env:
      SECRET_KEY: ${{ secrets.DJANGO_SECRET_KEY }}
      DB_NAME: dummy_test_db
      DB_USER: dummy_user
      DB_PASSWORD: dummy_password
      DB_HOST: dummy_host
      DB_PORT: 5432
      DEBUG: False

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v3

      - name: 🔧 Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 🏗️ Set up Node
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: 🧱 Install Backend Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r backend/requirements.txt

      - name: 🛠 Build React & Copy to Django
        run: |
          chmod +x build-frontend.sh
          ./build-frontend.sh

      - name: 📊 Collect Django Static Files
        run: |
          cd backend
          python manage.py collectstatic --noinput

      - name: 🧪 Run Tests (required)
        run: |
          cd backend
          python manage.py test --verbosity=2

      - name: 🚀 Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: ${{ env.AZURE_WEBAPP_NAME }}
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: backend

      - name: ✅ Deployment Success
        if: success()
        run: |
          echo "🎉 Deployment successful!"
          echo "🌐 Your app is live at: https://${{ env.AZURE_WEBAPP_NAME }}.azurewebsites.net"
          echo "👤 Admin: https://${{ env.AZURE_WEBAPP_NAME }}.azurewebsites.net/admin/"